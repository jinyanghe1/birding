# 腾讯云服务器部署指南

## 服务器信息

- **IP**: 124.223.171.149
- **配置**: 轻量应用服务器（40GB SSD，2GB RAM）
- **系统**: Ubuntu 22.04 LTS
- **访问地址**: http://124.223.171.149

## 架构

```
公网请求 → Nginx (80) → FastAPI (8765) → SQLite (birds.db)
                ↓
           静态文件（thumbs/）
```

## 服务管理

```bash
# 查看状态
sudo systemctl status birding

# 重启
sudo systemctl restart birding

# 查看日志
sudo journalctl -u birding -f

# 查看 Nginx 日志
sudo tail -f /var/log/nginx/access.log
sudo tail -f /var/log/nginx/error.log
```

## 更新流程

### 1. 代码更新

```bash
# 本地打包
cd /Users/hejinyang/WorkBuddy/观鸟skill
tar czf /tmp/birding.tar.gz --exclude='data' --exclude='models' --exclude='.git' \
  --exclude='__pycache__' --exclude='*.pyc' --exclude='logs' birdscan/

# 上传到服务器
scp -i ~/Downloads/birding.pem /tmp/birding.tar.gz ubuntu@124.223.171.149:~/

# 服务器上解压并重启
ssh -i ~/Downloads/birding.pem ubuntu@124.223.171.149
cd ~/birding && tar xzf ~/birding.tar.gz
sudo systemctl restart birding
```

### 2. 数据更新

```bash
# 本地重新导出
python -m birdscan.cli export-site

# 打包数据
cd data && tar czf /tmp/birding_data.tar.gz birds.db thumbs/

# 上传
scp -i ~/Downloads/birding.pem /tmp/birding_data.tar.gz ubuntu@124.223.171.149:~/

# 服务器上解压
ssh -i ~/Downloads/birding.pem ubuntu@124.223.171.149
cd ~/birding && tar xzf ~/birding_data.tar.gz
sudo systemctl restart birding
```

### 3. 一键更新脚本（推荐）

```bash
# 本地运行
cd /Users/hejinyang/WorkBuddy/观鸟skill
./scripts/deploy_to_server.sh
```

## 安全建议

1. **防火墙**：只开放 80/443/22 端口
   ```bash
   sudo ufw allow 80/tcp
   sudo ufw allow 443/tcp
   sudo ufw allow 22/tcp
   sudo ufw enable
   ```

2. **HTTPS**：申请免费 SSL 证书（Let's Encrypt）
   ```bash
   sudo apt install certbot python3-certbot-nginx
   sudo certbot --nginx -d your-domain.com
   ```

3. **SSH 密钥**：禁用密码登录
   ```bash
   sudo nano /etc/ssh/sshd_config
   # PasswordAuthentication no
   sudo systemctl restart sshd
   ```

4. **数据备份**：定期备份 birds.db 和 thumbs/
   ```bash
   # 每天凌晨 3 点备份
   0 3 * * * tar czf ~/backup/birding-$(date +\%Y\%m\%d).tar.gz ~/birding/data/
   ```

## 监控

- **磁盘**：`df -h`（40GB，当前用 13%）
- **内存**：`free -h`（2GB，Python 进程约 30MB）
- **进程**：`ps aux | grep birding`
- **日志**：`sudo journalctl -u birding -f`

## 故障排查

| 问题 | 排查 |
|---|---|
| 访问不了 | `sudo systemctl status birding` 看服务是否在跑 |
| 502 Bad Gateway | `sudo journalctl -u birding -n 50` 看报错 |
| 图片不显示 | `ls ~/birding/data/thumbs/` 看缩略图是否存在 |
| 数据库锁定 | `sudo systemctl restart birding` 重启服务 |

## 成本

- **服务器**：轻量应用服务器约 ¥24/月（腾讯云新人价）
- **带宽**：按流量计费，观鸟网站流量很小（预计 <1GB/月）
- **域名**：可选（约 ¥55/年）

## 下一步

1. **绑定域名**（可选）：买一个域名，解析到 124.223.171.149
2. **HTTPS**：申请 SSL 证书
3. **定时备份**：设置 cron 每天备份数据库
4. **监控告警**：配置磁盘/内存告警（腾讯云控制台）
